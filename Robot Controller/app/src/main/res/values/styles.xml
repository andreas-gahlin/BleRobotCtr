<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

<!-- Colors for TagMenu -->
    <style name="MyTabs">
        <!--<item name="android:actionMenuTextColor">@color/white_textcolor</item>-->
        <!--<item name="android:textColor">@color/white_textcolor</item> -->
        <!--<item name="android:background">@color/tabs_background</item>
        <item name="android:actionBarTheme">@color/white_textcolor</item>
        <item name="android:actionMenuTextColor">@color/tabs_background</item>-->
        <!--<item name="android:backgroundStacked">@color/red_action_bar</item>-->
    </style>

</resources>
